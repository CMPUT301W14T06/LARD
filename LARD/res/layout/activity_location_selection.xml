<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".LocationSelectionActivity" >

    <TextView
        android:id="@+id/locationSettingsTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Location Settings" />

    <CheckBox
        android:id="@+id/gpsLocationCheckbox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/locationSettingsTextView"
        android:onClick="gpsLocationClick"
        android:text="Use GPS Location" />

    <CheckBox
        android:id="@+id/customLocationCheckbox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/gpsLocationCheckbox"
        android:layout_below="@+id/gpsLocationCheckbox"
        android:onClick="customLocationClick"
        android:text="Use Custom Location:" />
    
    <ListView
        android:id="@+id/locationListView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/saveLocationButton"
        android:layout_below="@+id/customLocationCheckbox" />

    <Button
        android:id="@+id/saveLocationButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:onClick="locationSaveClick"
        android:text="Save" />

    <TextView
        android:id="@+id/locationTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/customLocationCheckbox"
        android:layout_alignBottom="@+id/customLocationCheckbox"
        android:layout_toRightOf="@+id/customLocationCheckbox"
        android:text="" />

</RelativeLayout>
